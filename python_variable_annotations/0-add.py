#!/usr/bin/env python3
"""The function add that takes a float a and a float b as arguments
and returns their sum as a float."""


def add(a: float, b: float) -> float:
    "add two float numbers"
    return (a + b)
